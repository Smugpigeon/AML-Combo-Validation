# Raw & processed data (large, external-sourced)
data/raw/
data/canonical/*.parquet
runs/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Secrets
.env
.env.local

# OS / editors
.DS_Store
.idea/
.vscode/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Large model outputs
*.pt
*.onnx
*.joblib
*.pkl
# Exception: the BeatAML RNA preprocessor bundle IS the kit artifact —
# it must ship with the canonical data so new patients can be featurized.
!data/canonical/beataml_rna_preprocessor.joblib

# v0.6 Phase 0 transient files (kept locally, not in git)
data/canonical/_drug_smiles_cache.json
data/canonical/*.csv.bak
data/canonical/chembl_drug_targets.cache.json
