# Current RR3 Cell Identity Audit

This directory records the first execution of the identity-before-perturbation
gate on `patient5`, `patient6`, and `patient12` (`11,652` cells total).

## Result

- `patient5`: blocked.
- `patient6`: blocked.
- `patient12`: provisional, not proven.
- Drug perturbation stage: locked.
- Combination prediction stage: locked.

No independently validated cell-level genomic or orthogonal identity calls are
present in the current compact H5AD/annotation package. Bulk mutations,
cytogenetics, expression suspicion, and calibrated blast priors were retained
as context but were not counted as proof.

The CSV and JSON files in this directory are generated by
`scripts/audit_rraml_cell_identity.py`.
